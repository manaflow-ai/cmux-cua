name: "CI: Rust Windows unit"

on:
  pull_request:
    paths:
      - "libs/cmux-cua/rust/Cargo.toml"
      - "libs/cmux-cua/rust/Cargo.lock"
      - "libs/cmux-cua/rust/crates/cmux-cua/**"
      - "libs/cmux-cua/rust/crates/cmux-cua-core/**"
      - "libs/cmux-cua/rust/crates/cmux-cua-testkit/**"
      - "libs/cmux-cua/rust/crates/platform-windows/**"
      - "libs/cmux-cua/rust/crates/cmux-cua-uia/**"
      - "libs/cmux-cua/tests/fixtures/**"
      - ".github/workflows/ci-rust-windows.yml"
  push:
    branches: [main]
    paths:
      - "libs/cmux-cua/rust/**"
      - "libs/cmux-cua/tests/fixtures/**"
      - ".github/workflows/ci-rust-windows.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-rust-windows-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  unit:
    name: Rust Windows unit and compile
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@9d47c6ad4b02e050fd481d890b2ea34778fd09d6 # v2
        with:
          workspaces: "libs/cmux-cua/rust -> target"
      - name: Run Windows Rust tests
        working-directory: libs/cmux-cua/rust
        # Compile every Rust target without executing desktop-dependent integration
        # tests; interactive behavior belongs in e2e-rust-windows.yml.
        run: cargo test -p cmux-cua -p cmux-cua-core -p cmux-cua-testkit -p platform-windows -p cmux-cua-uia --all-targets --no-run --locked

name: "CI: Rust Linux unit"

on:
  pull_request:
    paths:
      - "libs/cmux-cua/rust/Cargo.toml"
      - "libs/cmux-cua/rust/Cargo.lock"
      - "libs/cmux-cua/rust/crates/cmux-cua/**"
      - "libs/cmux-cua/rust/crates/cmux-cua-core/**"
      - "libs/cmux-cua/rust/crates/cmux-cua-testkit/**"
      - "libs/cmux-cua/rust/crates/platform-linux/**"
      - "libs/cmux-cua/tests/fixtures/**"
      - "nix/**"
      - "flake.nix"
      - "flake.lock"
      - ".github/workflows/ci-rust-linux.yml"
  push:
    branches: [main]
    paths:
      - "libs/cmux-cua/rust/**"
      - "libs/cmux-cua/tests/fixtures/**"
      - "nix/**"
      - "flake.nix"
      - "flake.lock"
      - ".github/workflows/ci-rust-linux.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-rust-linux-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  unit:
    name: Rust Linux unit and compile
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@9d47c6ad4b02e050fd481d890b2ea34778fd09d6 # v2
        with:
          workspaces: "libs/cmux-cua/rust -> target"
      - name: Install Linux build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            clang pkg-config libdbus-1-dev libpipewire-0.3-dev libspa-0.2-dev \
            libei-dev libxkbcommon-dev libx11-dev libxi-dev libxtst-dev libxext-dev
      - name: Run Linux Rust tests
        working-directory: libs/cmux-cua/rust
        run: cargo test -p cmux-cua -p cmux-cua-core -p cmux-cua-testkit -p platform-linux --all-targets --locked
      - name: Compile portable GNOME/KDE portal input
        working-directory: libs/cmux-cua/rust
        run: cargo check -p cmux-cua --features portal-input --locked
